<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Required for PyInstaller-bundled binaries (Mach-O ARM64 produced -->
    <!-- by Python's host Python install; signed by Tauri's bundler). -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Network — talk to localhost:8001 + outbound to Gemini/Claude APIs. -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- File access — user files (DICOM uploads, exports). -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Downloads + Documents — for Tier 2/4 archive + export paths. -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
  </dict>
</plist>
