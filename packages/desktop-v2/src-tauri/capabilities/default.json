{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window — minimal allowlist. Network calls go via fetch to localhost:8001 (proxied) or the configured server URL. Webview window creation is needed for the DICOM viewer (we open it as a separate Tauri window so it shares the sessionStorage tier the JWT is in, instead of in the system browser which has no auth context).",
  "windows": ["main", "dicom-*"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "core:webview:allow-create-webview-window",
    "core:window:allow-set-focus"
  ]
}
