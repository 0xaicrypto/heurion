{
  "name": "@heurion/ssrf-guard",
  "version": "1.0.0",
  "private": true,
  "description": "Shared SSRF-guarded remote download core — public-URL validation, pinned DNS, redirect loop, stream caps (#1074-1: worker/server 双实现收敛)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^7.0.2"
  }
}
